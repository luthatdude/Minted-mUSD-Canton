# Minted Protocol - ServiceMonitor Definitions
# INFRA-H-03: Prometheus service discovery for all Canton namespace services.
# Requires prometheus-operator CRDs (ServiceMonitor, PodMonitor).
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: canton-participant-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: canton-participant
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label to match deployment labels
      app.kubernetes.io/name: canton-participant
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
      scheme: http
      tlsConfig:
        insecureSkipVerify: false
  namespaceSelector:
    matchNames:
      - musd-canton
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-proxy-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: nginx-proxy
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label
      app.kubernetes.io/name: nginx-proxy
  endpoints:
    - port: metrics
      interval: 15s
      path: /nginx_status
      scheme: http
  namespaceSelector:
    matchNames:
      - musd-canton
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postgres-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: postgres
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label
      app.kubernetes.io/name: canton-postgres
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      scheme: http
  namespaceSelector:
    matchNames:
      - musd-canton
---
# PodMonitor for relay services (no Service object — metrics exposed on sidecar)
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: bridge-relay-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: bridge-relay
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label to match deployment labels
      app.kubernetes.io/name: bridge-relay
  podMetricsEndpoints:
    - port: health
      interval: 15s
      path: /metrics
  namespaceSelector:
    matchNames:
      - musd-canton
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: price-oracle-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: price-oracle
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label
      app.kubernetes.io/name: price-oracle
  podMetricsEndpoints:
    - port: health
      interval: 15s
      path: /metrics
  namespaceSelector:
    matchNames:
      - musd-canton
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: yield-sync-metrics
  namespace: musd-canton
  labels:
    app: minted
    component: yield-sync
spec:
  selector:
    matchLabels:
      # INFRA-H-01 FIX: Use standard K8s label
      app.kubernetes.io/name: yield-sync
  podMetricsEndpoints:
    - port: health
      interval: 30s
      path: /metrics
  namespaceSelector:
    matchNames:
      - musd-canton
