sdk-version: 2.8.0
name: ble-protocol
source: .
version: 1.0.0
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
build-options:
  - "--allow-redefinitions=no"
  - "--warn-unused-imports=no"
