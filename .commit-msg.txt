fix: align SDK 2.x artifacts to Daml 3.4.10 + migrate v1 to v2 JSON API

BREAKING: Canton JSON API v1 endpoints removed across all layers.

CI (.github/workflows/ci.yml):
- DAML_SDK_VERSION: 2.10.3 to 3.4.10 (matches daml/daml.yaml)

K8s (participant-deployment.yaml):
- Canton image: daml-sdk:2.9.3 to canton-community:3.4.10
- Remove standalone json-api sidecar (v2 API built into participant)

K8s (nginx-configmap.yaml):
- /v1/query to /v2/state/active-contracts + /v2/state/ledger-end
- /v1/fetch to /v2/version
- /v1/create + /v1/exercise to /v2/commands/submit-and-wait (unified)
- /v1/parties,packages to /v2/users,packages (blocked)
- Add /v1/ catch-all returning 410 Gone

Frontend (useCanton.ts):
- Full v1 to v2 API migration matching relay/canton-client.ts
- query: POST /v2/state/active-contracts with filtersByParty
- exercise/create: POST /v2/commands/submit-and-wait with commandId
- Add TemplateId parsing, userId/party tracking
- Remove @daml/ledger and @daml/types 2.x from deps
- Default port: 6865 to 7575

Agent docs: @daml/ledger to canton-client.ts / useCanton v2 refs

1717/1717 Hardhat tests passing, frontend tsc clean
